# Python
__pycache__/
*.pyc
*.egg-info/
dist/
build/

# uv / venv  (one per module under src/<module>/.venv; NB: commit each module's uv.lock)
.venv/

# per-module local env (the committed file is .env.example, not .env)
.env

# tool caches
.ruff_cache/
.mypy_cache/
.pytest_cache/
.coverage
htmlcov/

# artifacts
sbom.json
.DS_Store
