# {{MODULE}} environment — copy to .env (gitignored). All vars use the {{MODULE_ENV}}_ prefix
# (pydantic-settings env_prefix). Documented defaults match core/settings.py.

# Human-readable app name (FastAPI title).
{{MODULE_ENV}}_APP_NAME={{MODULE}}

# Enable debug mode (verbose errors). Never true in production.
{{MODULE_ENV}}_DEBUG=false
